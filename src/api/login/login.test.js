const { login } = require('./login');

describe('login', () => {
    it('should return a token', async () => {
        const req = {
            body: {
                email: ''
            }}
        })
});
test('login', () => {
    expect(login).toBeDefined();
});